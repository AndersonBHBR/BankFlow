# BankFlow Web

Portal Next.js 16 com BFF, cookie `HttpOnly`, proteção de mesma origem e módulos de Contas, Transferências e Observabilidade.

```bash
npm ci
npm run lint
npm run build
npm run dev
```

As URLs internas são configuradas pelas variáveis `BANKFLOW_*`. Veja `.env.example`.
